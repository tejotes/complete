micronaut:
  application:
    name: complete
greeting:
  stem: Hello
datasources:
  default:
    url: ${JDBC_URL:`jdbc:postgresql://nuc4010.fritz.box:5432/tejotes`}
    username: ${JDBC_USER:tejotes}
    password: ${JDBC_PASSWORD:Neptun73.}
    driverClassName: ${JDBC_DRIVER:org.postgresql.Driver}
jpa:
  default:
    packages-to-scan:
      - 'example.micronaut.domain'
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true